<div class="main posts-index">
  <div class="container">
    <% @posts.each do |f| %>
    <div class="posts-index-item">
      <%= link_to f.content, "/post/#{f.id}" %>
    </div>
    <% end %>
  </div>
</div>